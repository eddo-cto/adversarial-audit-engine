"""Test suite for the Adversarial Audit Engine core.

Stdlib only (unittest), consistent with the engine itself: the tests must run
anywhere the engine runs, with no install step.

    python3 -m unittest discover -s tests -t .
"""
